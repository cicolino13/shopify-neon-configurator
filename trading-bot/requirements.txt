pandas>=2.0
numpy>=1.24
PyYAML>=6.0
pytest>=7.4

# Optional, only needed for live paper-trading against a real MT5 terminal.
# The MetaTrader5 package only works on Windows (or Wine); backtesting does
# not require it.
# MetaTrader5>=5.0.45
